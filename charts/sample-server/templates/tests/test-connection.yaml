apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-server.fullname" . }}-test-connection"
  labels:
    {{- include "sample-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
