---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: postgresqls.dabase.example.com
spec:
  group: dabase.example.com
  names:
    kind: PostgreSQL
    listKind: PostgreSQLList
    plural: postgresqls
    singular: postgresql
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .spec.storage
      name: Storage
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: PostgreSQL is the Schema for the postgresqls API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgreSQLSpec defines the desired state of PostgreSQL
            properties:
              cpu:
                description: CPU defines the CPU size of PostgreSQL
                type: string
              memory:
                description: Memory defines the memory size of PostgreSQL
                type: string
              storage:
                description: Storage defines the storage size of PostgreSQL
                type: string
              version:
                description: Version defines the version of PostgreSQL
                type: string
            required:
            - cpu
            - memory
            - storage
            - version
            type: object
          status:
            description: PostgreSQLStatus defines the observed state of PostgreSQL
            properties:
              phase:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                  Phase defines the phase of PostgreSQL
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
