apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-server.fullname" . }}
  labels:
    {{- include "sample-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-server.selectorLabels" . | nindent 4 }}
